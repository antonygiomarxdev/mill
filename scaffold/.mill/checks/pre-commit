#!/bin/bash
# Mill gauntlet — pre-commit. Runs on every git commit.
# Fast checks (<30s). Fail = commit rejected.
# Which commands run is configured per project in .mill/gauntlet (see common.sh).
set -euo pipefail
DIR="$(cd "$(dirname "$0")" && pwd)"
source "$DIR/common.sh"

# Capability enforcement first — blocks files outside the active role's
# categories. No-op when no role is active (.mill/role absent).
"$DIR/role-enforce"

echo "mill: pre-commit gauntlet"
load_gauntlet

run_step build
run_step lint

echo "mill: pre-commit passed"
